# zenn

[zenn.dev/yusaka_lu](https://zenn.dev/yusaka_lu) に公開する記事のリポジトリ。構成・執筆コマンド・記事の規約は README.md にある。ここには**戻せない操作**と、**やってはいけない書き方**だけを書く。形の指定は README.md が持つ。同じことを2箇所に置かない。

## やらないこと

1. **`published: true` に勝手にしない。** `false` → `true` は公開の意思決定であって、記事の仕上げ作業ではない。ユーザーが明示的に指示したときだけ変える
2. **commit / push をユーザーの指示なしにしない。** `main` への push は Zenn が自動デプロイするので、**公開記事の書き換えと同義**。下書きの追加でも扱いは同じ
3. **公開済み記事のファイル名（slug）を変えない。** 記事 URL が変わり、外部からのリンクと Zenn 上のリアクションが切れる
4. **`npm run preview` の見た目を根拠に Mermaid を直さない。** プレビューの図は外部の iframe が描いていて読み込みごとに結果が変わる（詳細は README.md）。崩れて見えても、正しい構文を壊す方向に直すことになる。構文の判定は `npm run check:mermaid` だけを信じる
5. **サンプルコードへのリンクに `main` を使わない。** commit hash 付きの permalink にする。`main` を指すと、あとでコードを直したときに記事と実物がずれる

## 書くときにやらないこと

1. **検証していないことを断定する。** 試していない部分は「試していない」と書く。確かめた範囲を狭く言い切り、範囲の外は言い切らない
2. **意味のない三点セットを置く。** 「保守性・可読性・拡張性が向上します」のように、どれも測っていない語を3つ並べない
3. **見出しを同じ長さ・同じ品詞で揃える。** 目次が機械的に見える。長さも品詞も揃えにいかない
4. **各節を「まず」「次に」「最後に」で始める。** 順序は内容から読み取れる
5. **直近2本と同じ形にする。** タイトルの署名とリードの入り方は、書く前に `articles/` の直近2本を読んで、それと変える。同じ主体が同じ形式の入力から書き続けると、規約に書かれていない部分まで揃っていく

3〜5 は、1本ずつ読めば問題なく、一覧に並べたときだけ現れる。書き終えたら直近2本と並べて見る。

## 手元の記録との関係

記事の元になる調査・検証の記録は手元のナレッジベースにあり、**記事はそのコピーではない**。ナレッジベースは自分のための記録、記事は他人のための説明。同じ文章を2箇所に置かない。

| 手元に残すもの | 記事に書くもの |
| --- | --- |
| なぜそう考えたか、潰れた仮説、迷い | 読者が同じ結果に辿り着く手順 |
| 内部リンクで繋がる文脈 | 前提の明示と、外部への出典リンク |
| 結論だけの圧縮された記述 | 途中の出力、エラー、期待される結果 |

ナレッジベース内部のリンク記法（`[[slug]]`）を記事に持ち込まない。読者から辿れない。

## 言語

記事・コミットメッセージ・応答はすべて日本語で書く。技術用語と識別子は原文のまま。
