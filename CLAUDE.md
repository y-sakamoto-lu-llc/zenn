# zenn

[zenn.dev/yusaka_lu](https://zenn.dev/yusaka_lu) に公開する記事のリポジトリ。構成と執筆コマンドは README.md にある。ここには**戻せない操作**と、記事の型だけを書く。

## やらないこと

1. **`published: true` に勝手にしない。** `false` → `true` は公開の意思決定であって、記事の仕上げ作業ではない。ユーザーが明示的に指示したときだけ変える
2. **commit / push をユーザーの指示なしにしない。** `main` への push は Zenn が自動デプロイするので、**公開記事の書き換えと同義**。下書きの追加でも扱いは同じ
3. **公開済み記事のファイル名（slug）を変えない。** 記事 URL が変わり、外部からのリンクと Zenn 上のリアクションが切れる
4. **`npm run preview` の見た目を根拠に Mermaid を直さない。** 理由は次節
5. **サンプルコードへのリンクに `main` を使わない。** commit hash 付きの permalink にする。`main` を指すと、あとでコードを直したときに記事と実物がずれる

## Mermaid

図は Mermaid で書く。画像・SVG は貼らない（差分が追えず、あとで直せない）。

**プレビューの Mermaid は当てにならない。** Zenn の Mermaid は zenn-cli ではなく `https://embed.zenn.studio/mermaid` への iframe が描いている。同じ記事・同じバージョンでも読み込みごとに「正常」「極小に潰れる」「空白」が入れ替わり、**バージョンを上げても直らない**（0.5.2 と 0.5.3 で同じ挙動を確認、2026-08-28）。記事側の書き方の問題ではないので、**プレビューで崩れていても図を直してはいけない。** 正しい構文を壊す方向に直すことになる。

構文の判定はこれだけを信じる。

```sh
npm run check:mermaid
```

図の**見た目**の最終確認は本番デプロイ後になる、と割り切る。

## 記事の型

- タイトルは `<対象と行為> — <この記事にしかない切り口>`。`—` の後ろが書けないなら、記事の焦点がまだ絞れていない
- 前半に必ず3つ置く。**この記事で書かないこと / 検証環境の表 / 先に結論。** 読者が1画面目で離脱するかどうかはここで決まる
- **`## 自分では確かめていないこと` は `##` で立てる。** `###` に落とすと長い記事では埋没し、断定したのと同じことになる
- 失敗版を見せてから直す。完成形だけ出すと、読者は自分の失敗と記事のどこが違うのか照合できない
- コードブロックの前に1行「何のコードか」、後ろに1〜2行「どこを見るか」。裸のコードブロックは読み飛ばされる
- エラーと実行結果は原文のまま貼る。読者は自分の画面と1文字ずつ突き合わせる
- **検証していないことを断定しない。** 試していない部分は「試していない」と書く

## 手元の記録との関係

記事の元になる調査・検証の記録は手元のナレッジベースにあり、**記事はそのコピーではない**。ナレッジベースは自分のための記録、記事は他人のための説明。同じ文章を2箇所に置かない。

| 手元に残すもの | 記事に書くもの |
| --- | --- |
| なぜそう考えたか、潰れた仮説、迷い | 読者が同じ結果に辿り着く手順 |
| 内部リンクで繋がる文脈 | 前提の明示と、外部への出典リンク |
| 結論だけの圧縮された記述 | 途中の出力、エラー、期待される結果 |

ナレッジベース内部のリンク記法（`[[slug]]`）を記事に持ち込まない。読者から辿れない。

## 言語

記事・コミットメッセージ・応答はすべて日本語で書く。技術用語と識別子は原文のまま。
